[project]
name = "thesis-config"
version = "0.1.0"
description = "Thesis-service configuration: specialist peer URLs, arXiv MCP, refine loop."
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/thesis_config"]
